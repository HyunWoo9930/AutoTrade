apiVersion: batch/v1
kind: CronJob
metadata:
  name: stock-trading-evening
  namespace: default
spec:
  schedule: "40 15 * * 1-5"  # 평일 15:40 (한국시간)
  timeZone: "Asia/Seoul"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
          - name: evening-routine
            image: hyunwoo12/stock_trading:latest
            imagePullPolicy: Always
            command: ["python", "/app/run_evening.py"]
            envFrom:
            - secretRef:
                name: stock-trading-secret
            volumeMounts:
            - name: watchlist
              mountPath: /app/watchlist.py
              subPath: watchlist.py
            - name: journal
              mountPath: /app/data
          volumes:
          - name: watchlist
            configMap:
              name: stock-trading-config
          - name: journal
            persistentVolumeClaim:
              claimName: trading-journal-pvc